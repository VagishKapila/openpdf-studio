# COWORK Ledger — FormIQ (VagishKapila/formiq)

| # | Description | Branch | Status | Notes |
|---|---|---|---|---|
| 40.7 | Repo audit, rename to formiq, archive duplicate | staging | Shipped | Renamed openpdf → formiq, pwa-main frozen as v1.0.1, COWORK-LEDGER created |
| 41 | Auth API wiring — 7-commit functional auth build | staging | Shipped | Types, API client, Zustand persist store, shadcn UI primitives, LoginForm, SignupForm, GoogleButton, GoogleOAuthProvider, AuthDialog, UserMenu, RequireAuth, AppHeader gating |
| 41.A | Premium auth UI + brand identity drop-in | staging | Shipped | Real FormIQ wordmark + icon, cyan/teal/lime palette via brand.ts tokens, glass dialog with brand glow ring, UserMenu, RequireAuth gating, FormIQLogo in header, brand gradient Sign In CTA, favicon + manifest updated |
| 42 | Backend deploy — formiq-backend-staging on Railway | staging | Shipped | Express+TS+Drizzle+JWT+argon2, 10 auth routes, Railway Postgres, healthcheck ok |
| 43 | COWORK-43.A — Varshyl webhook 401 + PWA meta tag + Google OAuth env vars | staging | Shipped | Webhook X-Varshyl-Key fixed, theme-color meta corrected, VITE_GOOGLE_CLIENT_ID set on Railway |
| 44 | COWORK-44 — 5 UX bugs: password toggle, mobile dialog, tooltip toast, annotation flash, sig crop+presets | staging | Shipped | PasswordInput, AuthDialog overflow+padding, Sonner toast, imgCacheRef+cancelled flag, unified crop+S/M/L. Bundle index-BMxiUtmt.js. Layer 9 ✅ |
| 44.A | COWORK-44.A — 6 iPhone Layer 9 bugs fixed. Diagnosis: A=isDraggable excluded draw type + hardcoded false (both guards removed, dragend offsets points via konvaToPdf delta). B=crypto.randomUUID() generated new ID each open (annotations stored under old ID, lost on reopen); fixed with SHA-256 of first 64KB → deterministic ID. E=sizeMultiplier×naturalWidth all exceeded maxPdfW=200 cap (e.g. 300px×1.5=450→capped to 200); all three looked identical; fixed with absolute sub-cap widths S=80 M=150 L=240 CSS-px (S renders ~125pt, M/L cap at 200pt — distinct S vs M/L). C=focusin+scrollIntoView(350ms). D=hidden sm:flex/sm:inline. F=WHITE 240→220. Regression noted: PDF.js parentElement null errors appear on startup (see 44.B). iOS: Bug C verified DevTools only (not real iPhone). Bundle index-C0IG1bIn.js. Layer 9 ✅ |
| 44.A.1 | COWORK-44.A.1 — Hotfix: PDF.js AnnotationEditorLayer parentElement null regression | staging | Shipped ✅ | Root cause: SHA-256 persistence fix (44.A Bug B) made loadMostRecentDocument() reliably restore docs on startup, triggering PDF.js v4 built-in AnnotationEditorLayer initialization. Race condition with React DOM lifecycle → unhandled promise rejection `this.element.parentElement was null` (general.js:205). Fix: `annotationMode: 0` (AnnotationMode.DISABLE) added to both getDocument() call sites in apps/pwa/src/lib/loadPdf.ts (lines 29-30, 79-80). Commit c3582441. Bundle index-3trvNAqU.js confirmed live. annotationMode appears ×2 in bundle. Broken indicators: 0. Layer 9 ✅ 2026-04-28. |
| 45 | COWORK-45 — PDF Text Editing Tier 1 (Cover-and-Retype) | staging | Shipped ✅ | 5 commits (4 feature + 1 hotfix). Commit 1: 'edit' Tool type, ToolPalette SquarePen button, MobileToolbar flex-1 layout, AppShell keyboard E shortcut (49d62369ce). Commit 2: CoverAnnotation type+factory in annotations.ts, full AnnotationLayer drawing/rendering/auto-switch/touch path (bad9994f0e). Commit 3: CanvasArea first-use Sonner toast + formiq:hint:edit-tool-seen localStorage flag (4d52661f5a). Commit 4: exportPdf case 'cover' drawRectangle white rgb(1,1,1) opacity 1 (deb41c9a56). Hotfix: drawTouchMove had double `} else {` syntax error (two consecutive else-branches missing if-condition guards); fixed to `} else if (tool === 'highlight') {` and `} else if (tool === 'edit') {`; also fixed drawTouchEnd early-return guard to include 'edit' (95034a6d2f50). Build failed from bad9994f0e through beae701299 (8 consecutive Railway FAILs) until hotfix. Bundle index-DiTsYlvX.js confirmed live. createCoverAnnotation ×1, "cover" ×4, formiq:hint:edit-tool-seen ×2, case"cover" drawRectangle ve(1,1,1) ✅, keyboard E ✅. Layer 9 pending Vagish manual test on staging. |
